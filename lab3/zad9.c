#include <stdio.h>

int main()
{
	printf("Podaj liczbe a: ");
	scanf("%d", &a);
	printf("Podaj liczbe b: ");
	scanf("%d", &b);
	
	int pom=a;
	
	for(int i=0; i<=a+b; i++)
	{
		a/2
	}
	
	pom = b;
	for(int i=0; i<=a; i++)
	{
		if(taba[i]%2=0)
		
		pom = pom*2;
		tabb[i] = pom;
	}
	return 0;
}
